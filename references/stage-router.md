# Stage Router (Compatibility Wrapper)

This file has been superseded by the Skill GPS navigator.

For stage classification and routing, use:

- `references/navigator/lifecycle.md` — lifecycle enum and routing matrix
- `references/navigator/router-rules.md` — routing rules, confidence levels, and tie-break behavior

The original stage table, route depth guidance, and stage advancement gates now live in those navigator documents.

This file is kept for backward compatibility with existing tests and references. It does not contain its own lifecycle tables or routing logic.
